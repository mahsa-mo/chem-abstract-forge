# وضعیت فعلی پروژه — ChemAbstract Forge

> این فایل خلاصه وضعیت پروژه است. لطفاً قبل از هر تغییری این فایل را کامل بخوان تا کانتکست قبلی را داشته باشی و کارهای تکراری یا اشتباه انجام ندی.

## معرفی پروژه
یک وب‌اپلیکیشن که از روی متن مقالات شیمی، به‌صورت خودکار "چکیده تصویری" (Graphical Abstract) تولید می‌کند. سه حوزه اصلی: سنتز آلی، مواد و کاتالیست، بیوشیمی و مکانیسم دارو.

## تاریخچه پلتفرم
- پروژه ابتدا در **Lovable** ساخته شد.
- به دلیل تمام‌شدن اعتبار، به **Bolt.new** منتقل شد.
- گیت‌هاب (`mahsa-mo/chem-abstract-forge`) پل بین این دو پلتفرم است.
- **نکته مهم:** داشبورد Lovable هنوز متغیرهای محیطی Supabase را در سطح پلتفرم کنترل می‌کند. این یعنی برخی تغییرات از بولت ممکن است توسط Lovable بازنویسی/رونویسی بشن. اگر مشکلی مربوط به متغیرهای محیطی (env variables) یا تنظیمات Supabase بود، احتمالاً باید از داشبورد Lovable حل بشه، نه فقط از بولت.

## بک‌اند
- Supabase، شناسه پروژه: `exwemrdklkiagwrqedqw`
- جدول `generations` با ساختار parent-child (`parent_generation_id`) برای نگهداری نسخه‌های اصلی و ریجنریت‌شده
- تابع `create_generation` با چک اتمیک (atomic check-and-insert) و `SECURITY DEFINER` — منطق سهمیه باید سمت سرور باشه، نه در localStorage یا فقط سمت کلاینت
- Anonymous Sign-in و Google OAuth با قابلیت linkIdentity (اتصال هویت مهمان به گوگل) — پیاده‌سازی نسبی، هنوز UI کامل نشده

## مدل تولید تصویر
- قبلاً از گیت‌وی هوش مصنوعی Lovable استفاده می‌شد (`ai.gateway.lovable.dev` با `LOVABLE_API_KEY`) — این حالت لگاسی و منسوخ است.
- حالا مستقیم به Gemini گوگل وصل می‌شه (`generativelanguage.googleapis.com`) با `GEMINI_API_KEY`.
- فایل‌های کلیدی:
  - `src/lib/image-provider.ts` — تمام کد مخصوص پرووایدر تصویر باید فقط همین‌جا باشه (برای سوییچ راحت بین مدل‌ها در آینده)
  - `src/routes/api/generate-abstract.ts` — استریم SSE برای تولید تصویر

## قوانین مهم طراحی (رعایت بشه)
- هر تصویر اصلی، دقیقاً ۳ بار ریجنریت رایگان داره؛ این سهمیه کاملاً مستقل از سهمیه روزانه‌ست و وقتی تموم شد، دکمه ریجنریت کاملاً قفل می‌شه (سرریز به سهمیه روزانه نداره)
- تاریخچه همه‌ی تصاویر (اصلی و ریجنریت‌شده) باید برای همیشه نگه داشته بشه — هیچ‌وقت پاک نشه
- خطای API نباید سهمیه کاربر رو مصرف کنه (باید graceful error handling داشته باشه)
- همیشه قبل از تغییر کد، کد فعلی رو بخون (read-before-write)

## وضعیت فعلی (آخرین بروزرسانی)
- ✅ خطای "INVALID API KEY" حل شد
- 🔧 در حال بررسی: دکمه "تولید تصویر" در محیط پیش‌نمایش بولت غیرفعال (disabled) گیر کرده حتی با وارد کردن متن — در حال تشخیص علت (احتمالاً مرتبط با وضعیت سشن Anonymous Auth یا چک سهمیه)
- ⏳ باقی‌مانده: تکمیل UI اتصال هویت مهمان به گوگل (guest-to-Google linking)
- ⏳ تست کامل end-to-end برای Anonymous Auth

## نکات کلی برای هر تغییر
- تغییرات کد پرووایدر تصویر فقط در `src/lib/image-provider.ts` باشه
- منطق سهمیه و auth همیشه سمت سرور (Supabase RLS / functions)، نه سمت کلاینت
- هر تغییری که میدی، مختصر و در محدوده همون مشکل باشه — تغییرات گسترده و بی‌ربط نده
